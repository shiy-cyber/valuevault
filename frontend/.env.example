# URL del backend. En desarrollo se usa el proxy de Vite (/api), así que
# puede dejarse vacío. En producción (Netlify) apunta a tu backend en Railway/Render:
# VITE_API_URL=https://tu-backend.up.railway.app
VITE_API_URL=
